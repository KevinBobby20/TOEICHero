<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.toeichero.project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconfix"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkripsiProject">
        <activity
            android:name=".View.Gameplay.GameplayActivity"
            android:exported="false" />

        <service
            android:name="com.toeichero.project.Service.BackgroundMusic"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name=".View.Home.HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.SkripsiProject.NoActionBar" />
        <activity
            android:name=".View.Login.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".View.Login.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".View.Onboarding.CarouselActivity"
            android:exported="false" />
        <activity
            android:name=".View.Onboarding.SplashActivity"
            android:configChanges="orientation"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>